{%- extends '@EMSCH/ems_standard_template_ems/content_type/base_overview.twig' -%}

{%- block template -%}
    {%- set defaultColumns = true -%}
    {%- set columns = [
        { 'label': 'Name', 'template': (block('column_name')), 'orderField': 'template_name' },
    ] -%}
    {{ parent() }}
{%- endblock template -%}

{%- block column_name -%}
{%- verbatim -%}
    <a href="{{path('data.revisions', {ouuid: data.id, type: data.contentType} ) }}">{{ data.source.template_name }}</a>
{%- endverbatim -%}
{%- endblock column_name -%}